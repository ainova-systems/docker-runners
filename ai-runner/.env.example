# AI Automation Runner - Environment Configuration
#
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# ==============================================================================
# REQUIRED: GitHub Configuration
# ==============================================================================

# Full URL to your GitHub repository
GITHUB_REPOSITORY_URL=https://github.com/your-org/your-repo

# Runner registration token
# Get it from: Repository -> Settings -> Actions -> Runners -> New self-hosted runner
# NOTE: Tokens expire after 1 hour, generate a new one if needed
GITHUB_RUNNER_TOKEN=your-runner-token-here

# ==============================================================================
# API KEYS: Choose Your Approach
# ==============================================================================
#
# RECOMMENDED: Use GitHub repository secrets (not container env vars)
#   gh secret set ANTHROPIC_API_KEY
#   gh secret set CURSOR_API_KEY
#
# Benefits: Encrypted at rest, audit logs, easy rotation, single source of truth
#
# ALTERNATIVE: Set here for local development only (not recommended for production)
# ==============================================================================

# Claude CLI - API Key (only for local dev, use GitHub secrets in production)
# Get from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Claude CLI - OAuth Token (alternative to API key)
# Generate locally with: claude setup-token
# CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token-here

# Cursor CLI - API Key (only for local dev, use GitHub secrets in production)
# CURSOR_API_KEY=your-cursor-api-key-here

# ==============================================================================
# OPTIONAL: Runner Configuration
# ==============================================================================

# Custom runner name (default: ai-runner-<hostname>)
# RUNNER_NAME=ai-runner-prod

# Runner labels (default: self-hosted,claude-cli,cursor-agent)
# RUNNER_LABELS=self-hosted,claude-cli,cursor-agent,linux
